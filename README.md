# Maze Solver (Using A* Algorithm)

Maze Solver programed in python with pygame library, used A start algorithm to find the shortest distance.

## How to run

Inorder to run you need python installed in your machine.

Command to install pygame module
```
pip install pygame
```
command to run the maze-solver
```
python main.py
```
